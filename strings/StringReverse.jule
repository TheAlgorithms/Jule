# Main logic for Reverse string

module reverse_string

// Function to reverse a string
pub fn reverse(s: str) -> str {
    let mut reversed := ""
    for i := len(s)-1; i >= 0; i-- {
        reversed += s[i]
    }
    return reversed
}
