use tests::{TestStack}

pub fn string_package_tests(mut &tests: TestStack) {
    capitalize_tests(tests)
    is_alpha_tests(tests)
    is_digit_tests(tests)
    lower_case_tests(tests)
    reverse_tests(tests)
    upper_case_tests(tests)
}
