use tests::{TestStack}
use string::{is_digit}

fn is_digit_tests(mut &tests: TestStack) {
	tests.add("is_digit:1", fn(): bool {
		ret is_digit("12345") == true
	})

	tests.add("is_digit:2", fn(): bool {
		ret is_digit("foo") == false
	})
}
