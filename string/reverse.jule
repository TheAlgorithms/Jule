/*
This function, reverses the string.
*/

/*
Reverse("FooBar")
"raBooF"
*/

fn Reverse(mut s: str): str {
    let mut i = 0
    for i < len(s)/2; i++ {
        s[i], s[len(s)-i-1] = s[len(s)-i-1], s[i]
    }
    ret s
}