/*
This function, reverses the string.
*/

/*
Reverse("FooBar")
"raBooF"
*/

fn Reverse(mut s: str): str {
    let mut b = []byte(s)
    let mut i = 0
    for i < len(s)/2; i++ {
        b.swap(i, len(b)-i-1)
    }
    ret str(b)
}